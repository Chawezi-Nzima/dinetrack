import 'package:flutter/material.dart';

class KitchenHomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Kitchen Panel")),
      body: const Center(child: Text("Kitchen Staff Orders View")),
    );
  }
}
